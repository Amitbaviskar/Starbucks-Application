apiVersion: v1
kind: Service
metadata:
  name: starbucks-service
  namespace: starbucks
spec:
  type: NodePort
  selector:
    app: starbucks
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30080
